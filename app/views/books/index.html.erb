<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan = "3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |element|  %>
    <tr>
    <td><%= element.title %></td>
    <td><%= element.body %></td>
    <td><%= link_to "Show", book_path(element.id), method: :get %></td>
    <td><%= link_to "Edit",  edit_book_path(element.id) %></td>
    <td><%= link_to "Destroy", book_path(element.id), method: :delete,"data-confirm" => "Are you sure？" %></td>
    <% end %>
    </tr>
  </tbody>
</table>
<h2>New Book</h2>
<% if @book.errors.any? %>
  <div id = "error_explanation">
  <h2>
      <%if @book.errors.count>=2 %>
        <%= @book.errors.count %> errors prohibited this book from begin saved:
      <% else %>
        <%= @book.errors.count %> error prohibited this book from begin saved:
      <% end %>
  </h2>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
  </div>
<% end %>  
<%= form_with model: @book, url: books_path , method: :post do |f| %>
<div class = "field">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>
<div class = "field">
  <%= f.label :body %>
  <%= f.text_area :body %>
</div>
  <%= f.submit 'Create Book' %>
<% end %>
